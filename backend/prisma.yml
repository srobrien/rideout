#endpoint: ${env:PRISMA_ENDPOINT}
endpoint: https://rideout-prod-9d760fee20.herokuapp.com/rideout-backend/prod
datamodel: datamodel.graphql
disableAuth: true
hooks:
  post-deploy:
    - graphql get-schema -p prisma
