import styled from 'styled-components';

export const PlaceBar = styled.div`
  display: block;
  float: right;
  button {
    background: none;
    color: inherit;
    border: none;
    padding: 0;
    font: inherit;
    cursor: pointer;
    outline: inherit;
    i:hover {
      color: #da4545;
    }
  }
`;
